<?php

use Illuminate\Support\Facades\Route;

// Locales Area
Route::prefix('/{{name}}')->name('{{name}}.')->group(function () {
    Route::resource('locales', {{directory}}\LocalesController::class);
});

// {{name}} Area
Route::resource('{{name}}', {{directory}}\DashboardController::class);
{{is_active}}
{{image_link_remove}}
